{
    "$jsonSchema": {
        "bsonType": "object",
        "required": ["UserId"],
        "properties": {
            "UserId": {
                "bsonType": "int",
                "description": "must be an int and is required"
            }
        }
    }
}