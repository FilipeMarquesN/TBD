{
    "$jsonSchema": {
        "bsonType": "object",
        "required": ["BookId","UserId","Rating"],
        "properties": {
            "BookId": {
                "bsonType": "int",
                "description": "must be an int if provided"
            },
            "UserId": {
                "bsonType": "int",
                "description": "must be an int if provided"
            },
            "Rating": {
                "bsonType": "int",
                "description": "must be an int if provided"
            }
        }
    }
}