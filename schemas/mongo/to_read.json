{
    "$jsonSchema": {
        "bsonType": "object",
        "required": ["UserId","BookId"],
        "properties": {
            "UserId": {
                "bsonType": "int",
                "description": "must be an int if provided"
            },
            "BookId": {
                "bsonType": "int",
                "description": "must be an int if provided"
            }
        }
    }
}