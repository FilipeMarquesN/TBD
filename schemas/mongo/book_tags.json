{
    "$jsonSchema": {
        "bsonType": "object",
        "required": ["GoodreadsBookId","TagId","Count"],
        "properties": {
            "GoodreadsBookId": {
                "bsonType": "int",
                "description": "must be an int and is required"
            },
            "TagId": {
                "bsonType": "int",
                "description": "must be an int and is required"
            },
            "Count":{
                "bsonType": "int",
                "description": "must be an int and is required"
            }
        }
    }
}
