{
    "collection": "Users",
    "query": [
        {
            "$group": {
                "_id": "$Location",  
                "average_age": {"$avg": "$Age"},  
                "User_count": {"$sum": 1}  
            }
        },
        {
            "$sort": {"average_discount": 1}  
        }
    ]
}
