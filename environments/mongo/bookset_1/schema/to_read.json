{
    "$jsonSchema": {
        "bsonType": "object",
        "required": ["UserId","BookId"],
        "properties": {
            "UserId": {
                "bsonType": "int",
                "description": "must be an int and is required"
            },
            "BookId": {
                "bsonType": "int",
                "description": "must be an int and is required"
            }
        }
    }
}
